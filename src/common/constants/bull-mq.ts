export enum QueueName {
  STOCK_PRICE_QUEUE = 'stock-price-queue-local',
}

export enum QueueType {
  STOCK_PRICE_CRAWL = 'stock-price-crawl',
  STOCK_PRICE_CRAWL_DETAIL = 'stock-price-crawl-detail',
}
